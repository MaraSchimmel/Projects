Utilizes node.js as a search engine for words in Shakepeare plays

This program is a search engine for specific words in Shakespeare plays. The program utilizes node.js to act as a server to be accessed and read in the html file. The program only has access to three plays: 

The index.js server must be running before the index.html can access the server properly. The directory must be at the same level as index.html and index.js files. 